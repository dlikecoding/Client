import { ManageURLContextProvider } from "../../context/ManageUrl";

const Collection = (props: any) => <ManageURLContextProvider>{props.children}</ManageURLContextProvider>;

export default Collection;
